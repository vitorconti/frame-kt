import { z } from 'zod';
import type { MergeObjs, Result, SchemaError, SuccessResult } from './types.js';
declare function formatSchemaErrors(errors: z.ZodIssue[]): SchemaError[];
declare function isListOfSuccess<T>(result: Result<T>[]): result is SuccessResult<T>[];
declare function mergeObjects<T extends unknown[] = unknown[]>(objs: T): MergeObjs<T, {}>;
export { formatSchemaErrors, mergeObjects, isListOfSuccess };
